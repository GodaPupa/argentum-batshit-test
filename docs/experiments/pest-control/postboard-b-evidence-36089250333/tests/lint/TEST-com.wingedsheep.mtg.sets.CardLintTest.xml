<?xml version="1.0" encoding="UTF-8"?>
<testsuite name="com.wingedsheep.mtg.sets.CardLintTest" tests="3" skipped="0" failures="0" errors="0" timestamp="2026-09-25T03:19:15.298Z" hostname="runnervmtr4k5" time="20.38">
  <properties/>
  <testcase name="every card's pipeline variables, target bindings, and choice slots resolve" classname="com.wingedsheep.mtg.sets.CardLintTest" time="0.022"/>
  <testcase name="the lint allowlist contains no stale entries" classname="com.wingedsheep.mtg.sets.CardLintTest" time="0.001"/>
  <testcase name="print lint warnings for review (never fails)" classname="com.wingedsheep.mtg.sets.CardLintTest" time="0.009"/>
  <system-out><![CDATA[=== CardLinter warnings: 46 across 1 categories ===
--- OrphanPipelineWrite (46) ---
  [ARN] 'Sindbad': FilterCollection.storeMatching stores collection 'keptLand', but nothing reads it. Drop the store or wire up the consumer.
  [FEM] 'Orcish Spy': GatherCards.storeAs stores collection 'spied', but nothing reads it. Drop the store or wire up the consumer.
  [STH] 'Hermit Druid': GatherUntilMatch.storeMatch stores collection 'ignored', but nothing reads it. Drop the store or wire up the consumer.
  [INV] 'Do or Die': ChoosePile.storeOtherAs stores collection 'spared', but nothing reads it. Drop the store or wire up the consumer.
  [INV] 'Fight or Flight': ChoosePile.storeChosenAs stores collection 'canAttack', but nothing reads it. Drop the store or wire up the consumer.
  [INV] 'Global Ruin': SelectFromCollection.storeSelected stores collection 'kept', but nothing reads it. Drop the store or wire up the consumer.
  [INV] 'Stand or Fall': ChoosePile.storeChosenAs stores collection 'canBlock', but nothing reads it. Drop the store or wire up the consumer.
  [MRD] 'Proteus Staff': GatherUntilMatch.storeMatch stores collection 'proteusIgnored', but nothing reads it. Drop the store or wire up the consumer.
  [MRD] 'Spellweaver Helix': SelectFromCollection.storeSelected stores collection 'helixNamed', but nothing reads it. Drop the store or wire up the consumer.
  [MRD] 'Spoils of the Vault': GatherUntilMatch.storeMatch stores collection 'match', but nothing reads it. Drop the store or wire up the consumer.
  [RAV] 'Razia's Purification': SelectFromCollection.storeSelected stores collection 'kept', but nothing reads it. Drop the store or wire up the consumer.
  [RAV] 'Tunnel Vision': SelectFromCollection.storeSelected stores collection 'namedCard', but nothing reads it. Drop the store or wire up the consumer.
  [LRW] 'Broken Ambitions': ForEachCapturedController.countVariable stores stored number 'count0', but nothing reads it. Drop the store or wire up the consumer.
  [LRW] 'Fathom Trawl': GatherUntilMatch.storeMatch stores collection 'ignored', but nothing reads it. Drop the store or wire up the consumer.
  [ISD] 'Liliana of the Veil': ChoosePile.storeOtherAs stores collection 'keptPile', but nothing reads it. Drop the store or wire up the consumer.
  [GTC] 'Balustrade Spy': GatherUntilMatch.storeMatch stores collection 'matched0', but nothing reads it. Drop the store or wire up the consumer.
  [GTC] 'Consuming Aberration': GatherUntilMatch.storeMatch stores collection 'revealedLand', but nothing reads it. Drop the store or wire up the consumer.
  [KTK] 'Duneblast': SelectFromCollection.storeSelected stores collection 'saved', but nothing reads it. Drop the store or wire up the consumer.
  [LTR] 'Elven Farsight': SelectFromCollection.storeRemainder stores collection 'noncreature', but nothing reads it. Drop the store or wire up the consumer.
  [LTR] 'Mount Doom': SelectFromCollection.storeSelected stores collection 'saved', but nothing reads it. Drop the store or wire up the consumer.
  [LTR] 'Sméagol, Helpful Guide': GatherUntilMatch.storeMatch stores collection 'revealedLand', but nothing reads it. Drop the store or wire up the consumer.
  [LTR] 'The Ring Goes South': GatherUntilMatch.storeMatch stores collection 'lands', but nothing reads it. Drop the store or wire up the consumer.
  [WOE] 'Break the Spell': FilterCollection.storeMatching stores collection 'breakSpellYoursOrToken', but nothing reads it. Drop the store or wire up the consumer.
  [WOE] 'Faerie Slumber Party': FilterCollection.storeNonMatching stores collection 'faerieSlumberPartyOwnCreatures', but nothing reads it. Drop the store or wire up the consumer.
  [WOE] 'The End': ForEachCapturedController.countVariable stores stored number 'count4', but nothing reads it. Drop the store or wire up the consumer.
  [LCI] 'Unstable Glyphbridge': SelectFromCollection.storeSelected stores collection 'spared', but nothing reads it. Drop the store or wire up the consumer.
  [MKM] 'Deadly Cover-Up': ForEachCapturedController.countVariable stores stored number 'count5', but nothing reads it. Drop the store or wire up the consumer.
  [OTJ] 'Bucolic Ranch': SelectFromCollection.storeRemainder stores collection 'stayOnTop', but nothing reads it. Drop the store or wire up the consumer.
  [BLB] 'Wishing Well': SelectFromCollection.storeRemainder stores collection 'remainder', but nothing reads it. Drop the store or wire up the consumer.
  [FDN] 'Spinner of Souls': GatherUntilMatch.storeMatch stores collection 'ignored', but nothing reads it. Drop the store or wire up the consumer.
  [DFT] 'Demonic Junker': FilterCollection.storeMatching stores collection 'matching1', but nothing reads it. Drop the store or wire up the consumer.
  [TDM] 'Ainok Wayfarer': SelectFromCollection.storeRemainder stores collection 'leftInGraveyard', but nothing reads it. Drop the store or wire up the consumer.
  [FIN] 'Garnet, Princess of Alexandria': SelectFromCollection.storeRemainder stores collection 'garnetRest', but nothing reads it. Drop the store or wire up the consumer.
  [FIN] 'The Regalia': GatherUntilMatch.storeMatch stores collection 'matchLand', but nothing reads it. Drop the store or wire up the consumer.
  [TLA] 'Destined Confrontation': SelectFromCollection.storeSelected stores collection 'kept', but nothing reads it. Drop the store or wire up the consumer.
  [ECL] 'Aurora Awakener': GatherUntilMatch.storeMatch stores collection 'permanentsFound', but nothing reads it. Drop the store or wire up the consumer.
  [ECL] 'Aurora Awakener': SelectFromCollection.storeRemainder stores collection 'unchosenRevealed', but nothing reads it. Drop the store or wire up the consumer.
  [ECL] 'Winnowing': FilterCollection.storeMatching stores collection 'shareTypes', but nothing reads it. Drop the store or wire up the consumer.
  [TMT] 'Krang & Shredder': GatherUntilMatch.storeMatch stores collection 'krangMatch', but nothing reads it. Drop the store or wire up the consumer.
  [TMT] 'Krang & Shredder': GatherUntilMatch.storeMatch stores collection 'krangMatch', but nothing reads it. Drop the store or wire up the consumer.
  [TMT] 'Manhole Missile': SelectFromCollection.storeRemainder stores collection 'kept', but nothing reads it. Drop the store or wire up the consumer.
  [SOS] 'Mind Roots': MoveCollection.storeMovedAs stores collection 'discardedToGrave', but nothing reads it. Drop the store or wire up the consumer.
  [SOS] 'Page, Loose Leaf': GatherUntilMatch.storeMatch stores collection 'spell', but nothing reads it. Drop the store or wire up the consumer.
  [HOB] 'Cantankerous Keepers': FilterCollection.storeNonMatching stores collection 'rest', but nothing reads it. Drop the store or wire up the consumer.
  [HOB] 'Glamdring, Foe-hammer (face 1)': FilterCollection.storeNonMatching stores collection 'rest', but nothing reads it. Drop the store or wire up the consumer.
  [HOB] 'Part in Friendship': GatherUntilMatch.storeMatch stores collection 'ignored', but nothing reads it. Drop the store or wire up the consumer.
]]></system-out>
  <system-err><![CDATA[]]></system-err>
</testsuite>
