# Position 1 — Forge of Heroes qualification

Status: targeted qualification and exact generated-byte review passed at
`4aae1f9669769649e989735b4170f9bded15bc13`; independent source and artifact reviews
accepted that component scope. Full receiving-source CI and acceptance remain
pending. This is a card and shared-predicate capability gate with no gameplay admission.

The source starts from accepted bestow integration
`4ff03a9109eece49ab871b09c21b9533f66bb726`. The new `StatePredicate.IsCommander`
and `GameObjectFilter.commander()` read the existing commander designation in all
four evaluator contexts. They do not infer commander status from legend, name,
current type, control, or copied characteristics. Forge then composes existing
entry-history targeting and two independent projected-type counter conditions.

All 211 fetched remote refs were compared across the six SDK/engine paths before
editing. No commander predicate existed. Distinct fixed/snow/chosen-type,
entry-snapshot, skip-untap and other trigger/subtype changes stay on their existing
lineages; this patch adds isolated hunks to the receiving source. The external
Ferocity source owner has no concurrent delta on these paths. Its priority repair
is a separate receiving-source requirement and is not modified here.

The Scryfall C18 lookup, ascending printings, rulings, and exact image were fetched
on 2026-09-26. C18 #58, released 2018-08-10, is the earliest real printing. Both
published rulings (both counter kinds; neither counter kind) are preserved in the
definition and scenarios. The image request returned HTTP 200. A minimal C18 set
declaration enables ordinary discovery; no registry exception is added.

The push-only workflow first runs three commander-predicate cases and the
projection suite, then ten Forge scenarios and the existing 30 PDH/bootstrap/
readiness cases. The exact-card cases include both actual nonlegendary PDH
commanders, opposing control, ordinary same-name cards, real turn expiration,
projected either/both/neither types, a target leaving in response, and serialized
state/action replay. Layer and removal helpers are deterministic test fixtures.
They do not alter the frozen lists or count as matchup games.

Ordinary snapshot generation must produce only the new C18 Forge record. The
workflow retains it as an artifact, verifies snapshots again with updating
disabled, and checks lint and facade boundaries. No workflow commits a snapshot
or promotes the source. Review the generated bytes, publish those exact bytes,
then run full integration CI and the required independent evidence audit.

Expected registry gap after exact-card acceptance: **four** — Benevolent Blessing,
Opal Palace, Snake Umbra, Vines of Vastwood. All five existing full-game guards stay
closed. Executable pilots, typed full-game ledger extraction, public actor input,
complete-game replay and guarded execution still require receiving-source
qualification. The new SDK spelling has no Assay grammar addition in this scope.

Official counters remain 12 seeds generated, 0 consumed, 0 games initialized,
0 completed of 12, and 0 outcomes exposed. Hardware remains v0.7 / `KEEP_V07`.

## Targeted evidence checkpoint

Run `36249667143` executed 423 retained cases with zero failures, errors or skips:
commander predicates 3, projection 36, exact Forge 10, receiving PDH/bootstrap/
readiness 30, read-only snapshots 340, lint 3 and facade 1. Ordinary generation
changed only the new C18 record; its exact 4,475 bytes are bound by SHA-256
`8956f2db1a40917015600b29eece16803fb2c929d669a84661ed5d2cde0ceeaa`.
The [targeted receipt](evidence/forge-36249667143-targeted/receipt.json) retains
the complete original artifact and the independent Pest audit. Earlier dependency
and fixture-precondition failures remain preserved beside it. Publishing this
golden opens the broad receiving-source gate; it does not close that gate.

## Explicit type-fixture boundary

The three projected-type cases are synthetic inputs to Forge's resolution logic.
`SerializableModification.SetCardTypes` explicitly preserves subtypes; its lone use
in the earlier fixtures therefore left Human/Wizard on the noncreature inputs.
The successor fixture pairs it with the existing `SetCreatureSubtypes` operation:
planeswalker-only and land-only inputs have no creature subtypes, while the
creature/planeswalker input retains Human/Wizard. Exact equality of the complete
projected type collection and the separate subtype collection is now required,
as are the unchanged printed-creature identity and commander designation. The
same counter outcomes and ten scenarios remain mandatory. The earlier 49-case
rejected artifact and 423-case target artifact remain historical evidence of
their exact sources; they do not qualify these strengthened successor bytes.

This does not qualify general type/subtype removal. Production type changes must
supply their own complete semantics; a bare type operation is not evidence that
inappropriate subtypes disappear automatically. In the frozen opponent, Nyxborn
Hydra's bestow path uses `BestowComponent.auraType`, which replaces its type line
with `TypeLine.aura()` and preserves only supertypes. That existing pathway does
not use the synthetic `SetCardTypes` fixture. No policy restriction is introduced.
The dedicated successor runs only the ten changed exact-card scenarios; full
receiving-source CI and independent acceptance are still required afterward.
