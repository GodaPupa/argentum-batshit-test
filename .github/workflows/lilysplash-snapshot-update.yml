name: Temporary Lilysplash Snapshot Update

on:
  push:
    branches:
      - lilysplash/lab
    paths:
      - .github/workflows/lilysplash-snapshot-update.yml

permissions:
  contents: write

jobs:
  update-snapshots:
    if: startsWith(github.event.head_commit.message, 'Run temporary Lilysplash snapshot update')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v4
        with:
          ref: lilysplash/lab
          fetch-depth: 0
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
      - name: Generate card snapshots
        run: |
          chmod +x gradlew scripts/gradle-locked
          scripts/gradle-locked :mtg-sets:test --tests "*CardDefinitionSnapshotTest" -DupdateSnapshots=true
      - name: Verify exact snapshot scope
        run: |
          mapfile -t changed < <(
            {
              git diff --name-only
              git ls-files --others --exclude-standard
            } | sort -u
          )
          expected=(
            "mtg-sets/src/test/resources/snapshots/cards/ONE.json"
          )
          if [[ "${changed[*]}" != "${expected[*]}" ]]; then
            printf 'Unexpected snapshot scope:\n'
            printf '%s\n' "${changed[@]}"
            exit 1
          fi
      - name: Commit exact snapshot
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mtg-sets/src/test/resources/snapshots/cards/ONE.json
          git commit -m "Update Rustvine Cultivator snapshot"
          git push origin HEAD:lilysplash/lab
