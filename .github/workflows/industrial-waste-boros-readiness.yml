name: Industrial Waste — Boros Readiness

on:
  workflow_dispatch:
  push:
    branches:
      - industrial-waste/lab
    paths:
      - .github/workflows/industrial-waste-boros-readiness.yml
      - ai/src/test/kotlin/com/wingedsheep/ai/industrialwaste/IndustrialWasteBorosAdvisorModule.kt
      - ai/src/test/kotlin/com/wingedsheep/ai/industrialwaste/IndustrialWasteBorosPolicyAuditTest.kt
      - ai/src/test/kotlin/com/wingedsheep/ai/industrialwaste/IndustrialWasteBorosReadinessSmokeTest.kt
      - industrial-waste/control/industrial-waste-v1.0-submitted.dck
      - industrial-waste/gauntlet/boros-aggro-garrido-aboy-2026-09-12.dck
      - industrial-waste/input-identities.sha256
      - industrial-waste/sim/**
      - justfile

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  boros-readiness:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout exact branch head
        uses: actions/checkout@v6
      - name: Set up Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
      - name: Set up just
        uses: extractions/setup-just@v3
      - name: Validate frozen inputs
        run: |
          python3 -m unittest discover -s industrial-waste/sim -p 'test_*.py' -v
          python3 industrial-waste/sim/validate_inputs.py
      - name: Run deterministic Boros policy fixtures
        run: just test-class IndustrialWasteBorosPolicyAuditTest
      - name: Run exact-deck readiness smoke
        env:
          IW_BOROS_READINESS_SMOKE: "true"
        run: just test-class IndustrialWasteBorosReadinessSmokeTest
      - name: Print focused test failure details
        if: failure()
        run: |
          find ai/build/test-results/test -name 'TEST-*IndustrialWasteBoros*.xml' \
            -exec sed -n '1,260p' {} \; || true
      - name: Upload readiness evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: industrial-waste-boros-readiness
          path: industrial-waste/results/gate-5-boros-readiness-smoke-v1.json
          if-no-files-found: warn
