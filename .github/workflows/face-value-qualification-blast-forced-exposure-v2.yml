name: Face Value Qualification Blast Forced Exposure V2

on:
  push:
    branches: [face-value/lab]
    paths:
      - .github/workflows/face-value-qualification-blast-forced-exposure-v2.yml
      - experiments/face-value/qualification/apply_forge_profile_v4.py
      - experiments/face-value/qualification/audit_blast_forced_exposure_v2.py
  workflow_dispatch:

permissions:
  contents: read

jobs:
  forced-exposure-v2:
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@v4
        with:
          ref: face-value/lab
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Validate inputs and build profile v4
        run: |
          set -euxo pipefail
          python3 experiments/face-value/qualification/validate_inputs.py >/dev/null
          FORGE_SHA=f387ede550e336315637a8b765f0927ac768c113
          git clone --filter=blob:none https://github.com/Card-Forge/forge.git forge-src
          git -C forge-src checkout --detach "$FORGE_SHA"
          python3 experiments/face-value/qualification/apply_forge_profile_v1.py --forge-root forge-src >/dev/null
          python3 experiments/face-value/qualification/apply_forge_profile_v2.py --forge-root forge-src >/dev/null
          python3 experiments/face-value/qualification/apply_forge_profile_v3.py --forge-root forge-src >/dev/null
          python3 experiments/face-value/qualification/apply_forge_profile_v4.py --forge-root forge-src > forge-profile-v4-id.txt
          test "$(cat forge-profile-v4-id.txt)" = 'FACE_VALUE_QUALIFICATION_FORGE_PROFILE_V4'
          git -C forge-src diff > forge-profile-v4.patch
          cd forge-src
          mvn -B -DskipTests -pl forge-gui-desktop -am package
          chmod +x forge-gui-desktop/target/forge.sh
          rm -rf forge-gui-desktop/target/res
          cp -R forge-gui/res forge-gui-desktop/target/res
          echo "FORGE_LAUNCHER=$(pwd)/forge-gui-desktop/target/forge.sh" >> "$GITHUB_ENV"
      - name: Freeze new-seed forced-exposure replication
        run: |
          set -euo pipefail
          mkdir -p blast-fixtures blast-forced-v2-logs "$HOME/.forge/decks/constructed"
          cat > blast-fixtures/red-blast-caster-v2.dck <<'EOF'
          [metadata]
          Name=DIAGNOSTIC ONLY Red Blast Caster V2
          Deck Type=constructed
          [main]
          12 Pyroblast
          12 Red Elemental Blast
          12 Kessig Flamebreather
          24 Mountain
          EOF
          cat > blast-fixtures/blue-target-v2.dck <<'EOF'
          [metadata]
          Name=DIAGNOSTIC ONLY Blue Target V2
          Deck Type=constructed
          [main]
          24 Faerie Miscreant
          36 Island
          EOF
          cat > blast-fixtures/blue-blast-caster-v2.dck <<'EOF'
          [metadata]
          Name=DIAGNOSTIC ONLY Blue Blast Caster V2
          Deck Type=constructed
          [main]
          12 Hydroblast
          12 Blue Elemental Blast
          12 Faerie Seer
          24 Island
          EOF
          cat > blast-fixtures/red-target-v2.dck <<'EOF'
          [metadata]
          Name=DIAGNOSTIC ONLY Red Target V2
          Deck Type=constructed
          [main]
          24 Voldaren Epicure
          36 Mountain
          EOF
          python3 - <<'PY'
          from pathlib import Path
          rows = []
          seed = 730037
          for family in ('red-blasts', 'blue-blasts'):
              for seat in (1, 2, 1, 2):
                  rows.append(f'{family}\t{seat}\t{seed}')
                  seed += 1
          Path('blast-forced-v2-assignments.tsv').write_text('\n'.join(rows) + '\n', encoding='utf-8')
          PY
          cp blast-fixtures/*.dck "$HOME/.forge/decks/constructed/"
          sha256sum blast-fixtures/*.dck blast-forced-v2-assignments.tsv forge-profile-v4.patch > blast-forced-v2-inputs.sha256
          cat > blast-forced-v2-disposition.txt <<'EOF'
          DIAGNOSTIC_ONLY_NONLEGAL_BLAST_FORCED_EXPOSURE_V2_PROFILE_V4
          Seeds 730037-730044 and all outcomes are permanently retired diagnostic material.
          This is fresh-seed remediation, not a rerun or replacement of seeds 730029-730036.
          Results have no matchup, qualification, tuning, structural-diagnosis, or challenger authority.
          EOF
      - name: Run replication assignments
        run: |
          set -uo pipefail
          while IFS=$'\t' read -r family caster_seat seed; do
            if [ "$family" = red-blasts ]; then caster=red-blast-caster-v2.dck; target=blue-target-v2.dck; else caster=blue-blast-caster-v2.dck; target=red-target-v2.dck; fi
            if [ "$caster_seat" = 1 ]; then first="$caster"; second="$target"; else first="$target"; second="$caster"; fi
            log="blast-forced-v2-logs/${family}-caster-seat${caster_seat}-seed${seed}.log"
            timeout 240 "$FORGE_LAUNCHER" sim -d "$first" "$second" -n 1 -s "$seed" -c 120 > "$log" 2>&1
            code=$?
            if [ "$code" -ne 0 ]; then printf '\nDIAGNOSTIC_PROCESS_EXIT_%s\n' "$code" >> "$log"; fi
          done < blast-forced-v2-assignments.tsv
          exit 0
      - name: Audit profile-v4 blast capability
        id: audit
        continue-on-error: true
        run: |
          python3 experiments/face-value/qualification/audit_blast_forced_exposure_v2.py \
            --logs blast-forced-v2-logs --assignments blast-forced-v2-assignments.tsv \
            --output blast-forced-v2-summary.json
      - name: Preserve replication evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: face-value-blast-forced-exposure-v2
          path: |
            blast-forced-v2-summary.json
            blast-forced-v2-assignments.tsv
            blast-forced-v2-inputs.sha256
            blast-forced-v2-disposition.txt
            blast-fixtures/*.dck
            blast-forced-v2-logs/*.log
            forge-profile-v4.patch
            forge-profile-v4-id.txt
            experiments/face-value/qualification/input-validation-report.json
          if-no-files-found: warn
      - name: Enforce fail-closed replication gate
        if: steps.audit.outcome != 'success'
        run: exit 1
