name: Pest Control Tier-1 Relic Support

on:
  push:
    branches:
      - pest-control/tier1-postboard-support-relic
    paths:
      - .github/workflows/pest-control-tier-one-postboard-relic.yml

permissions:
  contents: write

jobs:
  qualify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: pest-control/tier1-postboard-support-relic
          fetch-depth: 0
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
      - uses: gradle/actions/setup-gradle@v6

      - name: Regenerate exact ALA snapshot
        run: ./gradlew :mtg-sets:test --tests "*CardDefinitionSnapshotTest" -DupdateSnapshots=true

      - name: Fail closed on unrelated snapshot drift
        shell: bash
        run: |
          set -euo pipefail
          changed="$(git diff --name-only)"
          printf '%s\n' "$changed"
          test "$changed" = "mtg-sets/src/test/resources/snapshots/cards/ALA.json"
          git diff --check

      - name: Qualify exact Relic behavior
        run: ./gradlew :mtg-sets:2008-2016:test --tests "*RelicOfProgenitusScenarioTest"

      - name: Qualify card lint
        run: ./gradlew :mtg-sets:test --tests "*CardLintTest"

      - name: Commit exact generated ALA bytes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mtg-sets/src/test/resources/snapshots/cards/ALA.json
          git commit -m "Synchronize Relic of Progenitus snapshot"
          git push origin HEAD:pest-control/tier1-postboard-support-relic
