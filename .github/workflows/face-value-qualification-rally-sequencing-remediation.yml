name: Face Value Qualification Rally Sequencing Remediation

on:
  push:
    branches: [face-value/lab]
    paths:
      - .github/workflows/face-value-qualification-rally-sequencing-remediation.yml
      - experiments/face-value/qualification/apply_forge_profile_v1.py
      - experiments/face-value/qualification/apply_forge_profile_v2.py
      - experiments/face-value/qualification/audit_rally_forced_exposure.py
  workflow_dispatch:

permissions:
  contents: read

jobs:
  rally-remediation:
    runs-on: ubuntu-latest
    timeout-minutes: 70
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - name: Verify frozen qualification inputs
        run: python3 experiments/face-value/qualification/validate_inputs.py
      - name: Build pinned Forge profile v2
        run: |
          set -euxo pipefail
          FORGE_SHA=f387ede550e336315637a8b765f0927ac768c113
          git clone --filter=blob:none https://github.com/Card-Forge/forge.git forge-src
          git -C forge-src checkout --detach "$FORGE_SHA"
          test "$(git -C forge-src rev-parse HEAD)" = "$FORGE_SHA"
          python3 experiments/face-value/qualification/apply_forge_profile_v1.py --forge-root forge-src > forge-profile-v1-id.txt
          test "$(cat forge-profile-v1-id.txt)" = 'FACE_VALUE_QUALIFICATION_FORGE_PROFILE_V1'
          python3 experiments/face-value/qualification/apply_forge_profile_v2.py --forge-root forge-src | tee forge-profile-id.txt
          test "$(cat forge-profile-id.txt)" = 'FACE_VALUE_QUALIFICATION_FORGE_PROFILE_V2'
          git -C forge-src diff > forge-profile-v2.patch
          sha256sum forge-profile-v2.patch experiments/face-value/qualification/apply_forge_profile_v1.py experiments/face-value/qualification/apply_forge_profile_v2.py > forge-profile-v2.sha256
          cd forge-src
          mvn -B -DskipTests -pl forge-gui-desktop -am package
          chmod +x forge-gui-desktop/target/forge.sh
          rm -rf forge-gui-desktop/target/res
          cp -R forge-gui/res forge-gui-desktop/target/res
          echo "FORGE_LAUNCHER=$(pwd)/forge-gui-desktop/target/forge.sh" >> "$GITHUB_ENV"
      - name: Freeze remediation fixture
        run: |
          set -euo pipefail
          mkdir -p diagnostic-decks diagnostic-logs "$HOME/.forge/decks/constructed"
          cat > diagnostic-decks/rally-forced-exposure.dck <<'EOF'
          [metadata]
          Name=DIAGNOSTIC ONLY Rally Forced Exposure
          Deck Type=constructed
          [main]
          16 Rally at the Hornburg
          16 Goblin Bushwhacker
          28 Mountain
          EOF
          cat > diagnostic-decks/passive-plains.dck <<'EOF'
          [metadata]
          Name=DIAGNOSTIC ONLY Passive Plains
          Deck Type=constructed
          [main]
          60 Plains
          EOF
          python3 - <<'PY'
          from pathlib import Path
          rows = [f'{1 + i % 2}\t{720161 + i}' for i in range(8)]
          Path('diagnostic-assignments.tsv').write_text('\n'.join(rows) + '\n', encoding='utf-8')
          PY
          cp diagnostic-decks/*.dck "$HOME/.forge/decks/constructed/"
          sha256sum diagnostic-assignments.tsv diagnostic-decks/*.dck forge-profile-v2.patch > diagnostic-inputs.sha256
          cat > diagnostic-disposition.txt <<'EOF'
          DIAGNOSTIC_ONLY_NONLEGAL_RALLY_SEQUENCING_REMEDIATION
          Seeds 720161-720168 and all outcomes are permanently retired diagnostic material.
          The decks are deliberately nonlegal and results have no matchup, tuning, structural-diagnosis, challenger, or qualification authority.
          A pass authorizes only an independent fresh-seed forced-exposure replication of profile v2.
          EOF
      - name: Run fresh remediation block
        run: |
          set -uo pipefail
          while IFS=$'\t' read -r fixture_seat seed; do
            log="diagnostic-logs/rally-forced-seat${fixture_seat}-seed${seed}.log"
            if [ "$fixture_seat" = 1 ]; then
              first='rally-forced-exposure.dck'; second='passive-plains.dck'
            else
              first='passive-plains.dck'; second='rally-forced-exposure.dck'
            fi
            timeout 120 "$FORGE_LAUNCHER" sim -d "$first" "$second" -n 1 -s "$seed" -c 60 > "$log" 2>&1
            code=$?
            if [ "$code" -ne 0 ]; then printf '\nDIAGNOSTIC_PROCESS_EXIT_%s\n' "$code" >> "$log"; fi
          done < diagnostic-assignments.tsv
          exit 0
      - name: Audit Rally sequencing remediation
        id: audit
        continue-on-error: true
        run: |
          python3 experiments/face-value/qualification/audit_rally_forced_exposure.py \
            --logs diagnostic-logs \
            --assignments diagnostic-assignments.tsv \
            --output rally-sequencing-remediation-summary.json
      - name: Preserve remediation evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: face-value-rally-sequencing-remediation
          path: |
            rally-sequencing-remediation-summary.json
            forge-profile-id.txt
            forge-profile-v1-id.txt
            forge-profile-v2.patch
            forge-profile-v2.sha256
            diagnostic-assignments.tsv
            diagnostic-inputs.sha256
            diagnostic-disposition.txt
            diagnostic-decks/*.dck
            diagnostic-logs/*.log
            experiments/face-value/qualification/input-validation-report.json
          if-no-files-found: warn
      - name: Enforce fail-closed remediation gate
        if: steps.audit.outcome != 'success'
        run: |
          echo 'Rally sequencing remediation was not demonstrated.'
          echo 'No replication or qualification seeds are authorized.'
          exit 1
