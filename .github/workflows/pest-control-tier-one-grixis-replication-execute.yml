name: Pest Control Tier-1 Grixis Replication Official Execution

on:
  pull_request:
    paths:
      - .github/workflows/pest-control-tier-one-grixis-replication-execute.yml
      - docs/experiments/pest-control/tier-one-grixis-replication-game1-incident.md

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm official replication workflow is terminal
        run: |
          set -euo pipefail
          echo "Run 35654021529 consumed the Game 1 attempt and is terminal."
          echo "No rerun, replay, replacement, or vector regeneration is authorized."
