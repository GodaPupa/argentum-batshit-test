# Isolated LKI deterministic evidence through attempt08

The latest targeted version passed73/73 assertions. The required broader rules regression failed14 of4,379 fresh assertions, so this source has **not** passed shared-engine acceptance. All research game counts remain zero.

`review.json` separates every attempt and records exact archive-member hashes. Extract `attempts.zip` and `historical-source-boundaries.zip` at the repository root to restore evidence paths. To reconstruct a test source, check out its recorded `source_head` in an isolated worktree and apply/extract the recorded source archive. Check every digest in that attempt's `compiled-inputs-before.json` before replaying its archived validator. Restore the archived validation helper beside the archived full-regression runner when applicable. Use the recorded just/JDK environment. Earlier cache failure01 is retained; later local runs used supported no-build-cache configuration.

Attempts01–04 predated automatic source ZIP capture. Their recovered source archives match every original changed or untracked source SHA256 exactly and preserve the original failed receipts. They were recovered from archived source bytes/patches, without rerunning tests.

Attempt08's raw receipt preserved9 earlier Toxin XML records without a2024 test-task marker. They are byte-identical to gate07 and predate gate08. They are **not** fresh test executions. The scope review retains the original raw receipt and reports4,379 fresh cases from the2 actually executed modules.

No game outcomes, pilot qualification, sanctioned tournament results, card admission, or Ferocity advantage is claimed here.
