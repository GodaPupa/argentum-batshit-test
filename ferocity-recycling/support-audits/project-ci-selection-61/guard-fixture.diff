--- before.py
+++ after.py
@@ -35,7 +35,7 @@
         self.addCleanup(self.root_patch.stop)
         self.addCleanup(self.build_patch.stop)
 
-    def invoke(self, task_status="", deleted_source=None, final_capture_failure=False):
+    def invoke(self, task_status="", deleted_source=None, final_capture_failure=False, xml_timestamp="CURRENT"):
         report = self.root / "evidence"
         arguments = ["validate_build.py", "--out", str(report), "--classes", self.test_class]
         if deleted_source:
@@ -56,7 +56,9 @@
             )
             xml = self.root / "rules-engine/build/test-results/test" / ("TEST-synthetic." + self.test_class + ".xml")
             xml.parent.mkdir(parents=True, exist_ok=True)
-            xml.write_text('<testsuite tests="1" failures="0" errors="0" skipped="0"/>')
+            timestamp = validation.stamp() if xml_timestamp == "CURRENT" else xml_timestamp
+            timestamp_attribute = f' timestamp="{timestamp}"' if timestamp is not None else ""
+            xml.write_text(f'<testsuite tests="1" failures="0" errors="0" skipped="0"{timestamp_attribute}/>')
             return SimpleNamespace(returncode=0)
 
         with contextlib.ExitStack() as stack:
@@ -85,6 +87,22 @@
         self.assertEqual(status, 0)
         self.assertTrue(receipt["stages"][0]["test_task_executed"])
         self.assertTrue(receipt["compiled_inputs_unchanged"])
+
+    def test_missing_xml_timestamp_is_retained_but_not_accepted(self):
+        status, receipt = self.invoke(xml_timestamp=None)
+        self.assertEqual(status, 1)
+        stage = receipt["stages"][0]
+        self.assertTrue(stage["test_task_has_execution_marker"])
+        self.assertEqual(stage["raw_xml_totals"]["tests"], 1)
+        self.assertEqual(stage["fresh_test_task_totals"]["tests"], 0)
+
+    def test_stale_xml_timestamp_is_retained_but_not_accepted(self):
+        status, receipt = self.invoke(xml_timestamp="2000-01-01T00:00:00+00:00")
+        self.assertEqual(status, 1)
+        stage = receipt["stages"][0]
+        self.assertTrue(stage["test_task_has_execution_marker"])
+        self.assertEqual(stage["raw_xml_totals"]["tests"], 1)
+        self.assertEqual(stage["fresh_test_task_totals"]["tests"], 0)
 
     def test_deleted_tracked_source_rejects_expected_head(self):
         missing = "rules-engine/src/main/kotlin/Deleted.kt"
